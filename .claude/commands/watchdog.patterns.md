---
description: Show crystallized strategy patterns for one project or all monitored projects
---

If the user supplied a project, call `watchdog_get_patterns` for that project.
If they did not, call `watchdog_get_patterns` with no project filter.

Summarize patterns in operator language with:
- project
- trigger or context
- tone
- observed success signal
- whether the pattern suggests leaning in or backing off

If no patterns exist yet, say that the system has insufficient evidence so far.